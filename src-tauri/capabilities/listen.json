{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "listen-capability",
  "description": "Allows listening to events",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen"
  ]
}
