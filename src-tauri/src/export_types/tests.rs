// src-tauri/src/export_types/tests.rs

use super::*;

#[test]
fn test_specta_builder_construction() {
    // Verifies that the command collection macro and types are valid
    let _builder = create_builder();
    // If it reaches here without panicking, the bridge definition is valid
}
